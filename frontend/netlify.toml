[build]
  base = "frontend/"
  command = "npm install && CI='' npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  REACT_APP_API_URL = "https://exam-prep-backend.render.com/api" 